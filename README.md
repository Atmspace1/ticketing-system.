# 🎫 ระบบจองตั๋วและกำหนดที่นั่ง (Ticketing System)

## 📋 เกี่ยวกับโครงงาน
**Ticketing System** คือระบบจองตั๋วออนไลน์ที่พัฒนาขึ้นโดยเน้นความน่าเชื่อถือและความยืดหยุ่นของระบบ โดยใช้หลักการ **Object-Oriented Programming (OOP)** อย่างเคร่งครัด เพื่อแก้ปัญหาการจองซ้ำซ้อน (Double Booking) และรองรับการคำนวณราคาที่หลากหลายตามประเภทลูกค้า

### 👥 สมาชิกกลุ่ม
1. **นาย ถิรวุฒิ แก้วหอมคำ** (1670700507)
2. **นาย อธิติ รุญเจริญวัฒนะ** (1670701695)
3. **นาย วิจักษณ์ ลาวรรณ์** (1670703626)
4. **นางสาว ชุดา เขียวหวาน** (1670700218)

---

## 🛠 Tech Stack
* **Language:** Java 17+
* **Framework:** Spring Boot 3.x (Web, Data JPA)
* **Database:** MySQL
* **Testing:** JUnit 5, Mockito
* **Frontend:** HTML5, CSS3, JavaScript (Fetch API)
* **Tools:** IntelliJ IDEA, Maven, Git

---

## 🎯 การประยุกต์ใช้หลักการ OOP (OOP Principles)

โปรเจกต์นี้ถูกออกแบบโดยใช้หลักการ OOP ทั้ง 4 ข้อ เพื่อให้โค้ดมีคุณภาพและดูและรักษาง่าย:

### 1. Encapsulation (การห่อหุ้ม)
* **การใช้งาน:** ในคลาส `Seat` เราทำการซ่อนสถานะ (`private SeatStatus currentStatus`) ไม่ให้ภายนอกแก้ไขได้โดยตรง
* **ประโยชน์:** การเปลี่ยนสถานะต้องผ่าน Method `holdSeat()` หรือ `confirmBooking()` เท่านั้น ช่วยป้องกันข้อมูลผิดพลาดและการจองซ้ำซ้อน

### 2. Abstraction (ความเป็นนามธรรม)
* **การใช้งาน:** สร้าง Interface `IBookable` เพื่อกำหนดมาตรฐานการทำงาน (Contract) ของระบบจอง เช่น `reserve()`, `confirmBooking()`, `cancelReservation()`
* **ประโยชน์:** แยก "สิ่งที่ระบบทำได้" ออกจาก "วิธีการทำงานจริง" ทำให้ง่ายต่อการแก้ไข Logic ภายในโดยไม่กระทบส่วนอื่น

### 3. Inheritance (การสืบทอด)
* **การใช้งาน:** การออกแบบโครงสร้างที่นั่ง (Seat Structure) ที่สามารถสืบทอดคุณสมบัติพื้นฐานจาก `SeatBase` (ในเชิงการออกแบบ Data Model)
* **ประโยชน์:** ลดความซ้ำซ้อนของโค้ด และง่ายต่อการเพิ่มประเภทที่นั่งใหม่ในอนาคต

### 4. Polymorphism (การมีหลายรูปแบบ)
* **การใช้งาน:** ใช้ **Strategy Pattern** ในการคำนวณราคาผ่าน Interface `PricingStrategy`
    * `RegularPricingStrategy`: ราคาปกติ
    * `MemberDiscountStrategy`: ลด 10%
    * `StudentDiscountStrategy`: ลด 15%
    * `SeniorDiscountStrategy`: ลด 20%
* **ประโยชน์:** สามารถเลือกใช้วิธีคำนวณราคาที่แตกต่างกันได้ทันทีตามประเภทลูกค้า (Runtime) โดยไม่ต้องแก้โค้ดหลัก

---

## 🚀 ขั้นตอนการติดตั้งและรันโปรเจกต์ (Installation)

### 1. Prerequisites (สิ่งที่ต้องมี)
* Java JDK 17 หรือสูงกว่า
* MySQL Server
* Git (สำหรับ Clone project)

### 2. เตรียมฐานข้อมูล (Database Setup)
เข้าไปที่ MySQL Workbench หรือ Command Line แล้วสร้าง Database:
sql
CREATE DATABASE ticketing_db;

3. ตั้งค่าการเชื่อมต่อ (Configuration)
เปิดไฟล์ src/main/resources/application.properties และแก้ไขรหัสผ่าน:

Properties

spring.datasource.url=jdbc:mysql://localhost:3306/ticketing_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=YOUR_PASSWORD  <-- ใส่รหัสผ่านของคุณตรงนี้

4. รันโปรเจกต์ (Run)
เปิด Terminal ในโฟลเดอร์โปรเจกต์ แล้วรันคำสั่ง:

Bash

./mvnw spring-boot:run
หรือกด Run TicketingSystemApplication ผ่าน IntelliJ IDEA

5. การใช้งาน (Usage)
เปิด Browser ไปที่: http://localhost:8080

ระบบจะสร้างข้อมูลจำลอง (Mock Data) ให้โดยอัตโนมัติ (T01 - T12)

🧪 การทดสอบ (Testing)
โปรเจกต์นี้รองรับการทำ Unit Test ด้วย JUnit 5 สามารถรัน Test ได้ด้วยคำสั่ง:

Bash

./mvnw test
📂 โครงสร้างโปรเจกต์ (Project Structure)
com.ticketing
├── config/             # Configuration & Data Initializer
├── controller/         # API Endpoints (BookingController)
├── model/              # Database Entities (Seat)
├── repository/         # Data Access Layer (SeatRepository)
├── service/            # Business Logic (BookingService, IBookable)
├── strategy/           # Pricing Strategies (Polymorphism)
└── TicketingSystemApplication.java  # Main Class

